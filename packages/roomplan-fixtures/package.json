{
  "name": "@propertyscan/roomplan-fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@propertyscan/contracts": "workspace:*",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@propertyscan/eslint-config": "workspace:*",
    "@propertyscan/typescript-config": "workspace:*",
    "@types/node": "^22.20.1",
    "eslint": "^9.28.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
